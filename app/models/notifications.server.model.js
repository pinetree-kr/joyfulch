'use strict';

/**
 * Module dependencies.
 */
var mongoose = require('mongoose'),
	Schema = mongoose.Schema;

/**
 * Notifications Schema
 */
var NotificationsSchema = new Schema({
	// Notifications model fields   
	// ...
});

mongoose.model('Notifications', NotificationsSchema);